You are an expert AI developer, DevSecOps engineer, and full-stack architect.  
You are tasked with auditing and improving my GitHub project: **medium-writeups**, which is a Go + Web + Docker powered project that auto-aggregates cybersecurity Medium articles into a website.  

### Your Mission:
1. **Repository Workflow Enhancements**
   - Review and fix `.github/workflows` (CI/CD) for:
     - Automated tests (Go, Node.js frontend, Docker build).
     - Security scans (CodeQL, Trivy, Dependabot).
     - Linting & formatting (golangci-lint, Prettier, ESLint).
     - Auto-deploy to GitHub Pages or my Dockerized Nginx container.
   - Add **semantic release** with changelogs + automated tagging.
   - Implement GitHub Actions caching (Go modules, Node.js dependencies).
   - Add a `Makefile` or `taskfile.yml` for common dev commands.
   - Ensure Docker builds are optimized (multi-stage builds, small base image).
   - Add pre-commit hooks for security & formatting.

2. **Code & Project Improvements**
   - Review **Go code (`main.go`)** for:
     - Error handling best practices.
     - Structured logging (Zap/Logrus).
     - Context & graceful shutdown handling.
     - Security (input validation, safe HTTP handling).
   - Review **frontend (index.html, enhance.js, sw.js, manifest.json)** for:
     - Accessibility (a11y).
     - Performance (Lighthouse audit improvements).
     - Offline/PWA enhancements (service worker caching, notifications).
   - Review **data handling**:
     - Ensure Medium feed parsing is safe against injection.
     - Add deduplication & caching layer (SQLite or Redis).
     - Add export APIs (JSON, RSS, Atom).

3. **Website Enhancements**
   - Add **modern UI** with TailwindCSS or shadcn/ui.
   - Improve **dashboard**:
     - Real-time threat analytics (charts via Recharts/D3).
     - CVE lookup integration (via NVD API).
     - Bug bounty stats (leaderboards, trending vulnerabilities).
   - Add **filters & search** (category, CVE ID, date).
   - Add **slides mode** (already present but refine animations/transitions).
   - Add **dark mode** toggle.
   - Add **RSS feed output** for external users.
   - Add **live API docs** (Swagger/OpenAPI for REST endpoints).

4. **Security Hardening**
   - Review `nginx.conf` for:
     - Security headers (CSP, HSTS, XSS protection).
     - Rate limiting / DoS protections.
   - Review `docker-compose.yml` & `Dockerfile` for:
     - Running as non-root.
     - Minimal attack surface (Alpine).
   - Add `security-policy.html` improvements:
     - Include bug bounty disclosure process.
     - Add contact methods for vulnerabilities.
   - Add automatic dependency scanning (npm audit, gosec).

5. **Documentation**
   - Rewrite `README.md` with:
     - Project overview + screenshots.
     - Setup instructions (Go, Node.js, Docker).
     - How to run locally & deploy.
     - Contribution guide + coding standards.
     - API endpoints documentation.
   - Update `PROJECT_SUMMARY.md` with high-level architecture.
   - Ensure `CONTRIBUTING.md` aligns with GitHub workflows.
   - Add **changelog.md** auto-generated.

6. **Extra Integrations**
   - Add Slack/Discord/Webhook notifications for:
     - New CVE posts.
     - New bug bounty writeups.
   - Add optional AI summary (Claude/GPT) of each article (stored in `data/`).
   - Add support for PDF/Markdown exports of weekly reports.
   - Add monitoring/analytics (Plausible/Umami, privacy friendly).

### Deliverables:
- Propose **concrete changes** in GitHub repo structure, workflows, and code.
- Write **improved workflow YAML files**.
- Suggest **UI/UX changes** with exact HTML/Tailwind snippets.
- Provide **Go code fixes** with security best practices.
- Give me a **step-by-step migration plan** for integrating enhancements.
